<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:CobranzaCostas.ViewModels"
             x:Class="CobranzaCostas.Views.Director.DirectorPage"
             x:DataType="vm:DirectorViewModel"
             Title="Tablero de Dirección">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Salir" Command="{Binding CerrarSesionCommand}" />
    </ContentPage.ToolbarItems>

    <ScrollView Padding="15">
        <VerticalStackLayout Spacing="20">
            
            <!-- Cabecera -->
            <Label Text="{Binding FechaHoy, StringFormat='Fecha: {0}'}" FontAttributes="Bold" FontSize="18" HorizontalOptions="Center" />
            <Label Text="{Binding NombreDirector, StringFormat='Director: {0}'}" FontSize="16" HorizontalOptions="Center" TextColor="Gray" />

            <!-- Indicador de carga -->
            <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" HorizontalOptions="Center" Color="#F44336" />
            <Label Text="{Binding MensajeValidacion}" TextColor="Red" HorizontalOptions="Center" FontAttributes="Bold" />

            <!-- SECCIÓN MÉTRICAS (En construcción) -->
            <Frame BorderColor="LightGray" CornerRadius="8" Padding="15" HasShadow="True">
                <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                    <Label Text="Métricas Globales (Nacional)" FontAttributes="Bold" FontSize="16" TextColor="#F44336" HorizontalOptions="Center" />
                    <Label Text="Vista en construcción..." FontAttributes="Italic" TextColor="Gray" HorizontalOptions="Center" />
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>